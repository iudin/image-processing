import { resolve } from 'path';

export const imgSize = 150;
export const inputDir = resolve(__dirname, '../input');
export const outputDir = resolve(__dirname, '../output');
export const imgFileTypes = ['jpg', 'jpeg', 'png'];
